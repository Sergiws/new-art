extends ../layout

block content
    section.container.mt-5
        .row.justify-content-center
            .col-md-6.col-lg-5
                .card.shadow
                    .card-body.p-4
                        h2.text-center.mb-4.text-primary Iniciar sesión como administrador
                        if error
                            .alert.alert-danger.text-center #{error}
                        form(method='POST', action='/login')
                            .mb-3
                                label.form-label(for='username') Usuario
                                input#username.form-control(type='text', name='username', required, placeholder='Ingresa tu usuario')
                            .mb-3
                                label.form-label(for='password') Contraseña
                                input#password.form-control(type='password', name='password', required, placeholder='Contraseña')
                            button.btn.btn-primary.w-100(type='submit') Iniciar sesión
